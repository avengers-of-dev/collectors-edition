#!/usr/bin/env ansible-playbook
- hosts: localhost
  connection: local
  tasks:
  - debug:
      msg: Check we are running in the directory of the script
    failed_when: not ansible_env.PWD.endswith('/007-ansible')

- hosts: localhost
  connection: local
  vars_prompt:
  - name: ansible_become_pass
    prompt: root password
  roles:
  - maintenance # performe some maintenance tasks first
  - aks
  - calibre
  - dotnet
  - firefox
  - forklift
  - homebrew
  - iterm2
  - keepassxc
  - licecap
  - mas
  - postman
  - skype
  - talisman
  - tiptoy
  - twitterrific
  # - visual-studio-code # not working, see: https://github.com/ansible-collections/community.general/issues/1037